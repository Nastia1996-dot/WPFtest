<UserControl x:Class="WpfTest.CounterControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfTest"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border BorderBrush="Black" BorderThickness="1" Padding="10">
        <!--<StackPanel>
            <TextBlock Text="Valore del contatore:"></TextBlock>
            <TextBlock Text="{Binding Count, RelativeSource={RelativeSource AncestorType=UserControl}}" FontSize="24" FontWeight="Bold"></TextBlock>
        </StackPanel>-->
        <DockPanel>
            <Button DockPanel.Dock="Top" Background="Yellow">TOP</Button>
            <Button DockPanel.Dock="Left" Background="Red">LEFT</Button>
            <Button DockPanel.Dock="Bottom" Background="BLUE">BOTTOM</Button>
            <Button DockPanel.Dock="Right" Background="Green">RIGHT</Button>
        </DockPanel>
    </Border>  
</UserControl>
    
